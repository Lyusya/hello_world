# hello_world 
это проект мавен, работающий с СУБД h2. Он реализует все базовые операции с базой данных.
Проект собирается командой "mvn package", а 
запускается командой "java -jar taskmaven-1.0.jar". Но данный файл не запустится без зависимостей, 
которые расположены в папке taskmaven-1.0.lib. 